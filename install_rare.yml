---
- name: cron-autossh-install
  hosts: renaissance
  become: yes

  vars_files:
    - secret_vars_common.yml

  tasks:
    - name: install cron
      apt:
        name: cron
        state: latest

    - name: install autossh
      apt:
        name: autossh
        state: latest
